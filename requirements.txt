# Model building requirements
numpy==1.24.3
pandas==1.5.3
scikit-learn==1.2.2
scipy==1.10.1
pytest==7.4.2
fastapi==0.103.0
pydantic==2.4.2
uvicorn==0.23.2
# fastapi==0.103.0 needs an old starlette (~0.27.x), but anyio is only a
# transitive dependency here, not pinned -- pip resolves "latest compatible"
# at build time. anyio 4.x shipped a lazy-import regression
# (AttributeError: module 'anyio' has no attribute 'to_thread') that breaks
# starlette.concurrency.run_in_threadpool, which FastAPI uses to run every
# synchronous `def` route handler -- i.e. every route in main.py. Pinned to
# the last pre-4.x release so a rebuild can't silently drift onto a broken
# anyio again.
anyio==3.7.1
hyperopt==0.2.7
mlflow==2.13.2
python-multipart==0.0.9
prometheus_fastapi_instrumentator
boto3==1.34.144
evidently==0.4.33
locust==2.29.1

# prediction_model/processing/lstm_autoencoder.py. CPU-only wheel explicitly
# (PyPI's default 'torch' wheel for Linux bundles CUDA and is ~10x larger) --
# this project trains/serves on a single CPU-only EC2 instance and GitHub's
# CPU-only Actions runners, so the CUDA build would only add image size/CI
# install time for nothing.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
